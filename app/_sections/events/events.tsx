import { SectionWrapper, SectionHeader } from "@/components";

export default function Events() {
  return (
    <SectionWrapper id="events" className="p-8 h-dvh">
      <SectionHeader>Events</SectionHeader>
    </SectionWrapper>
  );
}
