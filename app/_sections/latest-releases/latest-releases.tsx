import { SectionWrapper, SectionHeader } from "@/components";

export default function LatestReleases() {
  return (
    <SectionWrapper id="latest-releases" className="p-8 h-dvh">
      <SectionHeader>Latest Releases</SectionHeader>
    </SectionWrapper>
  );
}
