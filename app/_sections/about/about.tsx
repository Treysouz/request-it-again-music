import { SectionWrapper, SectionHeader } from "@/components";

export default function About() {
  return (
    <SectionWrapper id="about" className="p-8 h-dvh">
      <SectionHeader>ABOUT US</SectionHeader>
    </SectionWrapper>
  );
}
