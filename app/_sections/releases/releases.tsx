import { SectionWrapper, SectionHeader } from "@/components";

export default function Releases() {
  return (
    <SectionWrapper id="releases" className="p-8 h-dvh">
      <SectionHeader>Releases</SectionHeader>
    </SectionWrapper>
  );
}
