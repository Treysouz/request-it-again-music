import { SectionWrapper, SectionHeader } from "@/components";

export default function Contact() {
  return (
    <SectionWrapper id="contact" className="p-8 h-dvh">
      <SectionHeader>Contact Us</SectionHeader>
    </SectionWrapper>
  );
}
