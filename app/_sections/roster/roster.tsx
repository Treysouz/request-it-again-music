import { SectionWrapper, SectionHeader } from "@/components";

export default function Roster() {
  return (
    <SectionWrapper id="roster" className="p-8 h-dvh">
      <SectionHeader>Roster</SectionHeader>
    </SectionWrapper>
  );
}
