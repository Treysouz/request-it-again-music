import { SectionWrapper, SectionHeader } from "@/components";

export default function RecentEvents() {
  return (
    <SectionWrapper id="recent-events" className="p-8 h-dvh">
      <SectionHeader>Recent Events</SectionHeader>
    </SectionWrapper>
  );
}
